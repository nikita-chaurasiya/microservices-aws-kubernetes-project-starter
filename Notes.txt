ssh-keygen -t ed25519 -C "nikita.chaurasiya@gmail.com"

#enter then enter password 

eval "$(ssh-agent -s)"

ssh-add ~/.ssh/id_ed25519

# enter password given during 1st command 

cat ~/.ssh/id_ed25519.pub

# update SSH key in Git repo > profile >setting >SSh  new SSH key 

git config --global user.email "nikita.chaurasiya@gmail.com"
git config --global user.name "nikita-chaurasiya"

##Add AWS  IAM user in workpsace 

aws configure
AWS Access Key ID [None]: AKIA2XMMTF3X352G72H4
AWS Secret Access Key [None]: WWogIpAzyamYo78xUnrUgrniH9fw9/WeRSQWBpS
Default region name [None]: us-east-1
Default output format [None]: json

aws sts get-caller-identity

# Resume session 

aws eks --region us-east-1 update-kubeconfig --name my-cluster

kubectl config current-context
arn:aws:eks:us-east-1:737418620655:cluster/my-cluster

kubectl get svc

apt update


#Psql

apt update

apt install postgresql postgresql-contrib


